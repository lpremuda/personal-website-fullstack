import React, { useEffect } from 'react'

export default function Resume({ disableShowNavProjects }) {
  useEffect(disableShowNavProjects, [])

  return (
    <div>
      Resume page  
    </div>
  )
}
